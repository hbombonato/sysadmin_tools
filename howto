Global setup:
 Set up git
  git config --global user.name "hbombonato"
  git config --global user.email herlix@gmail.com
        
Next steps:
  mkdir sysadmin_tools
  cd sysadmin_tools
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:hbombonato/sysadmin_tools.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:hbombonato/sysadmin_tools.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue
